@model CfProjectAdmin.Models.UserRolesModel
   

@{
    ViewBag.Title = "Edit Roles";
}

<h2>Edit Roles</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <input name="UserId" id="UserId" type="hidden" value="@Model.User.Id" />
        <fieldset>

        <ul>
           @for (var i = 0; i < Model.AllRole.Count ; i++) 
            {
                @Html.HiddenFor(m=> Model.AllRole[i].Id)
                <li>
                    <input type="checkbox"
                           name="SelectedGroups" value="@Model.AllRole[i].Id" id="@Model.AllRole[i].Id"
                           @{if (Model.SelectedRole.Contains(Model.AllRole[i].Id)) { <text> checked='checked' </text>   } } />
                    <label for="@Model.AllRole[i].Id">@Model.AllRole[i].Name</label>
                </li>
    }
        </ul>
        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
    }

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

